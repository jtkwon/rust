// main is the first code that runs in every executable Rust program
fn main() {
    println!("Hello, world!") // This is rust macro.  Without ! is for functions
}

/**
 * Use rustc to compile.  It is similar to gcc or clang. 
 * Since rust is "ahead-of-time" compiled language, you can compile and give program to someone else
 * After compiling, Rust outputs a binary executable.
 * Example: rustc main.rs
 * /